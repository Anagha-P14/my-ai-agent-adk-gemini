## My AI Agent ADK ##

**agent.py:** The core logic file defining the agent’s identity, LLM configuration(Gemini), and system instructions.\
**__init__.py:** A package initializer that enables the ADK framework to discover and load the agent’s entry point.\
**.env:** A local environment file for managing sensitive credentials, including Project IDs, API keys, and regional settings.
